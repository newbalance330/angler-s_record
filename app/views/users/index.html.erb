<h1>ユーザー一覧</h1>

<% @users.each do |user| %>
<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "img/no_image.jpg", size:'200x200' %>
 <%= link_to user.name, user_path(user.id) %></td>
  <td><%= user.introduction %></td>
<% end %>