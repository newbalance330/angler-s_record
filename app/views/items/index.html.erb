<h1>アイテム一覧</h1>
<div class="container">
   <div class="row">
     <% @items.reverse.each do |item| %>
       <div class="col-lg-3 col-md-4 thumb">
         <%= link_to item_path(item) do %>
           <%= attachment_image_tag item, :item_image, :fill, 430, 300, format: 'jpeg', class: "fluid img-thumbnail" %>
         <% end %>
       </div>
     <% end %>
	 </div>
</div>